@page
@model InvWeb.Pages.Masterfiles.ItemSpecSteel.SteelOrigins.IndexModel

@{
    ViewData["Title"] = "SteelOrigins";
}

<h1>SteelOrigins</h1>

<p>
    <a asp-page="Create" class="btn btn-primary">Create New</a>
</p>
<div class="table-responsive card rounded-3 card-round-border">

    <table class="table custom-table-list rounded-3 overflow-hidden">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SteelOrigin[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SteelOrigin[0].Code)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model.SteelOrigin) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Code)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
    }
    </tbody>
</table>
</div>