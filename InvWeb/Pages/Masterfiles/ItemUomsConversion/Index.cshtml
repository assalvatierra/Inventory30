@page
@model InvWeb.Pages.Masterfiles.ItemUomsConversion.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.InvUomConversion[0].InvUomId_base)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InvUomConversion[0].InvUomId_into)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InvUomConversion[0].Factor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InvUomConversion[0].Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.InvUomConversion) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.InvUomId_base)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InvUomId_into)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Factor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./ConvItems/Index" asp-route-id="@item.Id">Items</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
